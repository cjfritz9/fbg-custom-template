export interface ProductMetadata {
  params: {
    handle: string;
  }
}

export interface BlogMetadata {
  params: {
    handle: string;
  }
}

export interface PartnerMetadata {
  params: {
    handle: string;
  }
}